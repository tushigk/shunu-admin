import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["picsum.photos", "images.unsplash.com", "backend.afterkiss.space", "ui-avatars.com", "localhost", "shunu.zurhai.com"],
  },
};

export default nextConfig;
